{
  "name": "website-fujipp-official",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "apps/frontend/fujipp-frontend",
    "apps/ai-chat-worker"
  ],
  "scripts": {
    "dev": "bun run --filter fujipp-frontend dev",
    "build": "bun run --filter fujipp-frontend build",
    "lint": "bun run --filter fujipp-frontend lint",
    "preview": "bun run --filter fujipp-frontend preview"
  },
  "dependencies": {
    "@fujipp/slipok": "^0.1.0"
  }
}
